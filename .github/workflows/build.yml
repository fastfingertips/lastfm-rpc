name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build Windows Application
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
      
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        
      - name: Install dependencies
        run: uv sync --dev
        
      - name: Build with Nuitka
        env:
          FILE_VERSION: ${{ github.ref_name }}
        run: uv run python tools/build.py


      - name: Archive Release
        uses: thedoctor0/zip-release@0.7.6
        with:
          type: 'zip'
          filename: 'lastfm-rpc-${{ github.ref_name }}-windows.zip'
          directory: 'dist/lastfm-rpc.dist'
          
      - name: Create Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/lastfm-rpc.dist/lastfm-rpc-${{ github.ref_name }}-windows.zip
          draft: false
          prerelease: false
